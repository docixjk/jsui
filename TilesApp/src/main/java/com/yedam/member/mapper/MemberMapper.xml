<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yedam.member.mapper.MemberMapper">
	<select id="login" resultType="MemberVO">
		SELECT * FROM members WHERE member_id=#{memberId} AND member_pw=#{memberPw}
	</select>
	
	<insert id="addMember" parameterType="MemberVO">
		INSERT INTO members(member_id, member_pw, member_name, member_phone, responsibility, image)
		VALUES(#{memberId},#{memberPw},#{memberName}, #{memberPhone}, 'user', #{image})
	</insert>
	
	<select id="memberList" resultType="MemberVO">
		SELECT * FROM members ORDER BY member_id
	</select>
	
	<select id="getMember" parameterType="string" resultType="MemberVO">
		SELECT * FROM members WHERE member_id=#{id}
	</select>
	
     <!-- SET member_name=#{memberName}, member_pw=#{memberPw}, member_phone=#{memberPhone}, member_addr=#{memberAddr} -->
	<update id="updateMember" parameterType="MemberVO">
     UPDATE members
    	<set>
    		<if test="image != null"> image = #{image},</if>
     		<if test="memberName != null"> member_name = #{memberName}, </if>
     		<if test="memberPw != null"> member_pw = #{memberPw}, </if>
     		<if test="memberPhone != null"> member_phone = #{memberPhone}, </if>
     		<if test="memberAddr != null"> member_addr = #{memberAddr}, </if>
     		<if test="responsibility != null"> responsibility = #{responsibility} </if>
    	</set>
     WHERE member_id=#{memberId}
    </update>
    
    <delete id="deleteMember" parameterType="int">
    	DELETE FROM members
    	WHERE member_id = #{mId}
    </delete>
	
</mapper>